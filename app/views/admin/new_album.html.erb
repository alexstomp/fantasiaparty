<h1 class="page-header"><strong>NOUVEAU ALBUM</strong></h1>

<%= form_for :album do |f| %>

    <div class="form-item">
        <label for=":date">Event Date</label>
        <%= f.date_field :date, :value => Date.today() %>
    </div>

    <div class="form-item">
        <label for=":age">Age</label>
        <%= f.number_field :age, :placeholder => "10" %>
    </div>

    <div class="form-item">
        <label for=":nom">Nom</label>
        <%= f.text_field :nom, :placeholder => "Prince/Princesse" %>
    </div>

    <div class="form-item">
        <label for=":description">Description</label>
        <%= f.text_area :description, :placeholder => "Petite description de la formule choisie..." %>
    </div>

    <div class="form-item">
        <%= f.submit "SAUVEGARDER", :class => "accent" %>
    </div>

    <div class="form-side">
        <p>D'abord, cree l'album pour l'evenement, il sera pas sur le site avant que tu le termine.</p>
    </div>

<% end %>